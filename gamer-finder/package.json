{
  "name": "gamer-finder",
  "version": "1.0.0",
  "description": "Gamer Finder",
  "main": "server",
  "scripts": {
    "start": "node server --ignore client",
    "develop": "cd client && npm run dev",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
